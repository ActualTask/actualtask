<article class="task">
  <section class="task-head">
    <h1><%= @task.title %></h1>

    <small>
      <time><%= @task.created_at.to_date.strftime("%d %B, %Y") %></time>
      <% if @task.category.present? %>
          |
          <%= link_to @task.category.name, @task.category %>
      <% end %>

    </small>
  </section>

  <section class="post-body">
    <%= @task.body.html_safe %>
  </section>
<section>
  kiasdfkjgh
  <%= render 'moderate/tasks/form' %>
</section>
</article>